export type View = "sections" | "assignments" | "people" | "requests" | "settings" | "home" | "my requests" | "surveys"

export const AdminViews = ["sections", "assignments", "people", "requests", "settings", "surveys"]

export const StudentViews = ["home", "my requests", "settings", "surveys"]